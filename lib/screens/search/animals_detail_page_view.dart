import 'package:flutter/material.dart';

class AnimalsDetailPageView extends StatelessWidget {
  const AnimalsDetailPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Animals detail Page'),
      ),
    );
  }
}
