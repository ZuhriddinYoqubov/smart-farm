import 'package:flutter/material.dart';

class FarmDetailPageView extends StatelessWidget {
  const FarmDetailPageView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FarmDetailPageView',), backgroundColor: Colors.blue,),
      
    );
  }
}